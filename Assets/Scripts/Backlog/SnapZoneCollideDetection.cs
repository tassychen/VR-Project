using UnityEngine;

public static class SnapZoneCollideDetection
{
    //public static SnapZone GetAvailableSnapZone(Vector3 position, float radius)
    //{
    //    Collider[] hits = Physics.OverlapSphere(position, radius);
    //    foreach (Collider hit in hits)
    //    {
    //        SnapZone snapZone = hit.GetComponent<SnapZone>();
    //        if (snapZone != null && !snapZone.IsOccupied)
    //        {
    //            return snapZone;
    //        }
    //    }
    //    return null;
    //}
}
